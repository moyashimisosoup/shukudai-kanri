# 作業を始める前に読むこと

このリポジトリは、AIエージェント（Claude Code / Codex）が交代で作業する。
交代のたびに同じ失敗を繰り返さないよう、前提を一か所にまとめてある。

## 1. 引き継ぎメモを先に読む

**`CLAUDE_HANDOFF.md`** に、設計の意図・ハマりどころ・作業中の采配が入っている。
名前は `CLAUDE_` で始まるが、Claude と Codex の共用メモである。

このファイルは `.gitignore` に入れてあり、公開リポジトリには含まれない。
手元に無ければ、利用者に確認すること。無いまま推測で進めないこと。

作業を終えたら、次に触る人がハマる前提を書き足すこと。

## 2. 公開リポジトリである

このリポジトリは公開されている。次のものは絶対に書かない。

- 合言葉（同期用のパスフレーズ）の実物
- 子どもや家族の名前、学校名、宿題の記録内容などの個人情報
- APIキー・トークン・パスワードなどの秘密情報
- ローカルの絶対パス（利用者名が含まれるため）

## 3. 変更したら必ず通すこと

```
npm test
```

テストが5件ほど落ち、`... is not defined` が出るときは、コードではなく
作業ツリーの改行コードを疑う。詳しくは `CLAUDE_HANDOFF.md` を見ること。

## 4. 配信番号と公開版番号は別のもの

- **配信番号**（`?v=20260822g` のような値）… ブラウザに新しいファイルを
  読み直させるための内部の番号。`assets/` や `index.html` を変えたら、
  `index.html` と `tests/sync-regression.test.js` の版数表を同じ値に上げる。
  片方だけだとテストが落ちる。
  ただし、コメントだけの変更は上げない（実行時に何も変わらないため）。
- **公開版番号**（`1.8.0` のような値）… 利用者へ伝える変更の単位。
  上げ方は `VERSIONING.md` に従う。**指示が無いかぎり上げないこと。**
  公開のタイミングは利用者が決める。

## 5. 画面の切りかえは URL の `#` で行っている

`routeFromHash()` は知らない名前を子ども画面に落とす。ページ内リンクなどで
`#` を書きかえると、戻る・再読みこみ・ホーム画面から開き直したときに
保護者が子ども画面へ飛ばされる。**`#` を汚さないこと。**

## 6. 子ども向けと大人向けで言葉が違う

同じものを、子ども画面ではかな（「かならず やる」）、保護者ページでは漢字
（「必須の宿題」）で呼ぶ。どちらを触っているのか確かめてから直すこと。
片方だけの変更を求められることが多い。
