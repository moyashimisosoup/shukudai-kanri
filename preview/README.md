# preview/ — 検討中のサンプル置き場

採用が決まっていない案を、実機（とくにスマホ）で見るためだけに置く場所。

- **配信物ではない。** `assets/` と `index.html` からは参照しない。
  ここに何を置いても、公開版番号も内部配信番号も上げない。
- **どこからもリンクしない。** 各ページに `noindex,nofollow` を入れること。
  URL を知っている人だけが開ける状態にしておく。
- **本体の画面と間違われない書き方にする。** 冒頭に「検討用のサンプル」と
  書き、本体への戻り道を1つ置く。
- **採用・不採用が決まったら消す。** フォルダごと消してよい。

置いてよいのは、公開して困らないものだけ。合言葉、家族の記録、
個人情報、秘密情報を含むものは置かない（`AGENTS.md` の2節と同じ）。

## いま置いてあるもの

| ファイル | 中身 | 状態 |
|---|---|---|
| `kettei.html` | **校了版。** 4段階（毎回／A項目／B分類／C完全制覇） | **校了（2026-08-23）** |
| `anim-lab.html` | きらきら3案・完全制覇の特別演出・6テーマのコントラスト監査 | 途中の検討版 |
| `hanabi.html` | 花火のかたち5案（線香花火／打ち上げ／柳／型抜き／文字） | 途中の検討版 |
| `hikari.html` | 光り方から作り直した版（スターマイン／きらきら／ボケ玉） | 途中の検討版 |

**いま見るべきは `kettei.html` だけ。** ほかの3つは、そこへ至る過程で
作った検討版。なぜその形になったのかを追うときだけ開けばよい。

確定仕様は `docs/completion-animation-design.md` にある。
**4b（本体組み込み）はそちらを発注書として使う。**

## 4b が済んだら

`preview/` はフォルダごと消してよい。ただし消す前に、必要な値が
`docs/completion-animation-design.md` へ写っているか確かめること
（このフォルダは公開されているが配信物ではないので、消しても
公開版番号・内部配信番号のどちらも動かさない）。
