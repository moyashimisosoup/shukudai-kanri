# バージョン番号の運用ルール

しゅくだいノートの公開版は、`メジャー.マイナー.パッチ` の3つの数字で表します。
最初の一般公開版を `1.0.0` とし、公開する変更内容に応じて次のように番号を上げます。

## 3つの数字

- **メジャー（左）**: 保存データや共有方法の互換性がなくなる変更、または利用者に移行操作を求める大規模な変更。例: `1.4.3 → 2.0.0`
- **マイナー（中央）**: 既存データとの互換性を保った機能追加、または操作の流れが変わる大きな改善。例: `1.3.2 → 1.4.0`
- **パッチ（右）**: 不具合、誤記、表示、操作性、アクセシビリティ、性能、安全性の修正。例: `1.3.1 → 1.3.2`

複数の変更を一度に公開するときは、含まれる変更のうち最も大きい区分を採用します。
メジャーを上げたらマイナーとパッチを0へ、マイナーを上げたらパッチを0へ戻します。

## 公開時の扱い

1. 番号を上げるのは、利用者が開ける公開サイトへ変更を反映するときです。
2. 公開前の作業、テストだけの変更、同じ内容の再配信では公開版番号を上げません。
3. 同じ日に複数回公開した場合も、変更単位ごとに別の番号を付けられます。
4. 変更履歴には、日付、公開版番号、利用者への影響を短く記録します。
5. `assets/app.js`、`index.html`、`package.json`、`package-lock.json`、`start/updates.html` の番号を同時に更新し、テストで一致を確認します。

## 内部配信番号

`20260814h` のような値は、ブラウザに新しいファイルを確実に読み込ませ、端末間で配信状態を確認するための内部配信番号です。公開版番号 `1.3.2` とは用途が異なるため、変更履歴の採番には使いません。

内部配信番号だけを更新して同じ内容を再配信する場合、公開版番号は据え置きます。

