# 引継ぎ

最終更新: 2026年8月17日（v1.3.22 公開時点）

**次のセッションへ**：積み残しは無い。控え [`BACKLOG.md`](BACKLOG.md) も空。
直前にやったのは
[高学年向けの呼びかけ文の全面見直し](#済んだ高学年向けの呼びかけ文の見直しv1322)で、
そこに 触るときの縛りと、今回 決めた 境界の規則 が書いてある。

## いまの状態

- ブランチ `codex/multi-household-public` → `public-households/main` へ push 済み
- 公開版 **v1.3.22**、キャッシュ版 `20260817e`
- テスト **229件 全パス**（`node --test tests/*.test.js`）
- アプリの中身に未公開の変更なし

## テストの走らせ方（本人からの指示 2026-08-17）

**200件規模を毎回まわさないこと。**資源の消費が大きい。
変更した所に絞って名前で走らせ、**全件は公開直前に一度だけ**。

```bash
node --test --test-name-pattern '<語>' tests/sync-regression.test.js
```

## 公開手順（毎回これ）

1. `assets/app.js` の `RELEASE_VERSION`
2. `index.html` の `application-version` と `?v=` のキャッシュ版（style.css / app.js）
3. `package.json` と `package-lock.json`（2か所）
4. `start/updates.html` に履歴を1行足し、「バージョン番号の見方」の版も直す
5. `tests/sync-regression.test.js` の「公開版番号v…をそろえる」テストを更新
6. 全件テスト → `git push public-households codex/multi-household-public:main`

## 判断基準

憲章 [`docs/PRODUCT_POLICY.md`](../../PRODUCT_POLICY.md) の **5章「操作の手ごたえと確認」** が本文。要点だけ:

- 安全な操作に確認を挟まない（確認は「危険がある」と読まれる）
- 待てるものは聞かない（継ぎ目まで待てば中断の選択肢も要らない）
- 平常時の画面に要素を足さない
- 「元に戻す」は時系列ではなく場所（欄ごと）で持つ
- 失われないものを「失われる」と警告しない
- 既にある事実で判定できるなら、覚えることを増やさない
- 規則を破るときは規則を消さず、**理由つきの例外**として登録する

## 検査の型（実際に取り逃がした事象から）

- 単体で切り出す検査は**呼び出し経路の欠落を見つけられない**。「関数が正しい」と
  「関数が呼ばれる」は別。実機で通すまで分からない
- テストのハーネスが**既定値で本番の条件を回避していないか**疑う
- 検査スクリプトの結果は**数で妥当性を確かめる**（配当表の抽出ミスは字数で気づいた）
- 検査を足したら、**落ちるべきものが落ちるか**を一度試す。通っているだけの検査は
  効いている証拠にならない（配当外の字の混入、案内文の巻き戻しで実際に確認した）
- **同じ文字列が2か所にある所を1つの正規表現で見ない。** v1.3.22 で `data-no-reading` の
  検査が これで すり抜けた ―― 一言と完了予測の両方に同じ指定があり、片方を消しても
  もう片方に当たって通ってしまった。**要素ごとに**見る。5通り壊して試して見つけた
- **引継ぎに書いた縛りが、実際に効いているとは限らない。** 「大人側の幅は13.25以下」と
  書いてあったが、テストは `PACE_MESSAGES` しか見ておらず 大人側は素通りだった。
  縛りを書くときは、それを見ているテストの場所も併せて書く
- CSSでは「規則が**在る**」と「規則が**勝つ**」が別。規則の存在だけを見るテストは
  後から書かれた上書きに無力で、実際 v1.3.21 の作業中に
  **テスト全通過のまま実機だけ直っていない**状態を踏んだ（`.set-task-move` が
  後方の `@media (max-width:639px)` に負けていた）。幅ごとの指定は
  **いちばん後ろのブロックに置き**、「後ろに戻す指定が無いこと」も併せて見る

## 直近で入れた仕組み（触るとき注意）

| 仕組み | 場所 | 注意 |
|---|---|---|
| 版の自動取り込み | `checkForNewVersion` / `adoptNewVersionIfSafe` / `noticeAdopted` | 起動と前面復帰の継ぎ目でだけ動く。経過時間で止めない（起動直後に走るため無効化される）。ループ防止は `K_UPDATE_RELOADED_FOR` |
| 宿題編集の取り消し | `configTaskBase` / `TASK_FIELD_KEYS` / `refreshTaskRow` | 基準は**最初の変更の直前**に控える（`toggle` では取りこぼす）。`change` の分岐は `saveCfg()` だけで描き直さないものが多く、`refreshTaskRow` を呼ばないと印が出ない |
| 学年で言い方を変える | `grownUpWording()` / `wording()` / `PACE_MESSAGES_ADULT` | 小4以上と9（漢字のまま）が対象。**大人側の漢字は小4までの配当**に収める。収まらない語は `{漢字\|よみ}` と書いてルビ。例外は `STAMP_KANJI_EXCEPTIONS`（いまは「了」＝スタンプ「完了！」のみ） |
| 書きかけの保護 | `sheetInputsChanged` / `confirmLeaveSheet` / `popstate` | シートを開くとき履歴を1つ足す。閉じたら `history.back()` で回収 |
| 長い文字列の出し分け | `paceVerdictSizeClass` / `appTitleSizeClass` | 1行を保ったまま長い文だけ縮める型。文字幅は字ごとの重みで見積もる（空白.35、約物.55）。CSS側は `clamp()` で画面幅にも追従させ、**幅ごとの @media より後ろに置いて上書きする** |

## 文字の切れ（…）についての裁定（2026-08-17 に全数を実測）

`text-overflow:ellipsis` を使う7か所を 320px の実機で測り、直す／残すを分けた。
**基準は「文言を自分たちが書いているか」**。自分たちの文なら入る形にする責任があり、
利用者が入れた文字列は長さを保証できないので … を残す。

| 箇所 | 実測 | 裁定 |
|---|---|---|
| `.set-task-summary strong`（設定の宿題名） | 欄が108pxしかなく実データ16件中ほぼ全部が62〜114px超過 | **直した**。`@media (max-width:639px)` で名前に1行まるごと使わせ、種類・件数・並べかえは下の段へ。16件すべて超過0 |
| `.topband-title`（上帯の題名） | 名前4文字（「さくらこの夏休みの宿題」）で17px超過 | **直した**。`appTitleSizeClass` で11文字以上を縮める。320pxで12文字まで収まる。下限14pxは隣の日づけ（13px）より大きく保つ線。13文字以上は … のまま（帯を2行にしないための割り切り） |
| `.sec-head h2`（子ども画面） | 最長「まいにち すこしずつ」で超過0 | 残す。文言は自分たちのもので現状は入る。大人側は既に折り返し済み |
| `.pace-verdict` | 全文言で超過0（`paceVerdictSizeClass` が効いている） | 残す。… は見積もりが外れたときの気づき用 |
| `.parent-head h2` | 3種類の固定文、超過0 | 残す。縦に割れると帯の丈が伸びるため、意図して … にしてある |
| `.free-said`（自由記述カードのメモ1行目） | 箱が246pxのところ**475pxに膨らみ**カード外へ。要素自身は溢れていないので**…すら出ない** | **直した**。`.free-body{ min-width:0 }`。カードは grid の `1fr` で最小幅が min-content のため、nowrap の中身がトラックを押し広げていた |
| `.parent-share-short`（保護者ページ上部の共有バッジの狭い画面用の文） | 「子ども端末の接続待ち」が**20px超過**、「ほかの端末と共有中」が9px超過 | **直した**。既にある「：設定」と同じ「：〈短い語〉」の形にそろえた（`：接続待ち`／`：ほかの端末`／`：子ども`／`：〈名前〉`）。名前だけは長さを約束できないので … に任せる |

**この2件は最初「残す」と誤って裁定した。**画面に出ていないことを理由に実測せず、
設計上の省略だろうと推測したため。指摘を受けて出し方（`#settings` を開く／
実カードに差し込む）を用意して測ったら、どちらも実際に壊れていた。
**出ていない画面は「問題が無い」ではなく「まだ見ていない」。**

## 済んだ：高学年向けの呼びかけ文の見直し（v1.3.22）

**何が問題だったか。** 大人側の32文は、低学年の文を漢字に置きかえただけだった。
語彙は大人びるが 文の骨格は幼いまま残り、体言止めの言い切り（「まず〇〇から！」）が
12/32 を占めていた。言い切りは統制的な口調として読まれ、年齢が上がるほど反発を生む。
裏づけが数字でも出ていた ―― 低学年側は幅の上限 13.25 に張りついていたのに、
大人側は max 10.90。**短かったのは制約ではなく、変換の副作用**だった。

### 決めたこと（3つ。本人の裁定）

1. **条件つき提案を基本の型にする。**「まようなら〇〇から」「一つだけ手をつけよう」。
   選ぶ余地を残したまま行き先を示す。遅れているとき（hurry）は責める語を全廃し、
   「まずは5分だけと思って始めるのがコツ」のように**着手そのものを目標**にする
2. **境界は文法で決める。** 子に**語りかけている文**（問いかけ・依頼・勧誘・状態の言い当て）
   だけを切りかえる。名詞句の見出し（「さいごの しあげ」「✓ ほぞんずみ」）は骨組みとして残す。
   **保護者ページは一切触らない**
3. **1行に収まらない文は縮めずに2行にする。** 下限14pxは、隣の日づけ13px・下の完了予測12pxより
   大きく保つための線。ここを割ると画面でいちばん大事な一言がいちばん読みにくくなる

### 触る前に知っておくこと（すべてテストで縛ってある）

- **各状態8文ずつ。** 数を変えると「同じ日は同じ文言」（`day % rows.length`）が崩れる
- **漢字は小4までの配当。** 収まらない語は `{漢字|よみ}` と書いてルビにする
  （`rubyHTML` が組む）。ルビの読みは**かなだけ**、かつ**配当外の語にだけ**振る
  ―― 読める語にまで振るとうるさい。どちらもテストが見ている
- **`data-no-reading` は大人側だけ。** 低学年側の文には「宿題」が入っており、
  小1・小2には機械のかな化（`applyReadingDisplay`）が**必要**。同じ属性が学年で逆に働く
- **幅は 26.5 まで（2行ぶん）。** 13.25 を超えると `pace-verdict--wrap` で折り返す。
  3行になるとテストが落ちる
- **ルビの読みを幅に数えない。** `{余裕|よゆう}` は画面では2文字ぶん。素直に数えると
  10幅の文が16幅と誤判定され、短い文が不要に縮む（`paceVisualWidth`）

### この仕組みが何をしているか

「読める漢字」で **小4以上**または**漢字のまま（9）** を選んだ家では、
子どもへの**呼びかけの文だけ**を 少し大人びた言い方に 切りかえる。

| もの | 場所 |
|---|---|
| 対象かどうかの判定 | `grownUpWording()`（`g >= 4 \|\| g === 9`） |
| 文の選択 | `wording(child, adult)` |
| 進み具合の一言 | `PACE_MESSAGES` ／ `PACE_MESSAGES_ADULT`（good/focus/hurry/steady × 各8文） |
| スタンプ | `stamp(wording('できた！', 'できた'))` の形で各所に散っている |

- **指すものが無い比喩を入れない。**「いいリズムだね」は何のリズムか無く、削除済み

### 進め方

```bash
node --test --test-name-pattern '大人びた言い方|ルビ|よゆう' tests/sync-regression.test.js
```

- 文言を変えたら **実機でも見る**。テストは文字列と配当しか見ておらず、
  「画面でどう見えるか」は見ていない（v1.3.21 で 実際に 2回 取り逃がした）

## 済んだこと（掘り返さないための覚え書き）

- **`start/` の漢字レベル説明は更新済み。** 3か所（`getting-started.html` の
  「名前と、読める漢字」と「何年生向けですか。」、`index.html` のFAQ）はいずれも
  「すべてひらがな」から「小学6年生まで」、それに「漢字のまま」。
  **直っていないと思って書きかえないこと。** 旧文を期待する assertion も無い
- 上帯の差し替え（`nav-bar__inner` / `nav-links` / `mobile-menu`）も対応済み。
  この件で走っていた背景タスクは中断済みで、**重複作業は発生していない**
- 案内文を実装に縛るテストを2件追加済み（下記）。`readingOptions()` の
  選択肢を増減させると、案内文を直すまで落ちる

| テスト | 見ているもの | 効くことの確認 |
|---|---|---|
| 案内ページの「読める漢字」の説明は、実際に選べる範囲と食い違わない | `readingOptions(-1)` を実行して**端の3つ**（先頭・学年の上端・最後）を取り出し、その一文が両ページに在るか | 案内側を旧文に戻す／実装側に7を足す、どちらでも落ちる |
| 「読める漢字」の学年は、端から端まで飛ばさずに並ぶ | 「AからBまで」という言い方が暗に約束している**連続性**を実装側で | 小3を抜くと落ち、欠けた位置（3）を名指しする |
