# 宿題ノート：引き継ぎメモ（Claude / Codex 共用）

更新日: 2026-08-11（JST）

このメモは、AIエージェント（Claude Code / Codex）が交代で作業するための共有メモです。
作業を終えたら、変更点と「次に触る人がハマりやすい前提」をここへ書き足してください。

**このファイルは公開リポジトリに入っており、公開版サイトのURLからも読めます。**
追記した内容はそのまま一般に公開されます。次のものは絶対に書かないでください。

- 合言葉（同期用のパスフレーズ）の実物
- 子どもや家族の名前、学校名、宿題の記録内容などの個人情報
- APIキー・トークン・パスワードなどの秘密情報
- ローカルの絶対パス（`C:\Users\...` など。利用者名が含まれるため）

書いてよいのは、コードの構造・設計の意図・ハマりどころ・作業手順など、
「次に触る人が知らないと壊す前提」に限ります。

## 現在の状態（2026-08-11・ここを正とする）

下の追記には、その時点では正しかった未完了事項や旧仕様も履歴として残っている。
再実装や再判断を避けるため、現状の確認はまずこの節と `README.md` を見ること。

- 最新公開コードは `bc290a4`。公開版・家庭用版とも app / style / tokens / kanji / sync は
  `20260811z`。全テストは107件。詳細は末尾の各2026-08-11追記を参照。
- 共有内容は AES-GCM による E2E 暗号化済み。進捗は値ごとの時刻、削除は墓標を使って
  合流する。旧説明の「暗号化なし」「max / ORだけ」「訂正不可」は現仕様ではない。
- 大人向け導線は `#settings`（保護者用ページ）/ `#tasks`（宿題を決める）/
  `#config`（アプリの設定）の3ページ。
- IPADIC 条文の同梱、通常同期用Firebaseルール、進捗サマリーは完了済み。90日無更新の
  共有データは手動管理ツールと墓標方式を実装済み。ただし墓標対応ルールはまだ本番未適用で、
  実データの削除も未実施。活動時刻を自動記録するCloud Functionも未デプロイ。
  「きょうはなんの日？」は一次資料確認を再開済み。個別の古い「次にやること」
  「残っている宿題」は履歴であり、この一覧より優先しない。

## 公開先・リポジトリ

- 公開版: https://moyashimisosoup.github.io/shukudai-notebook/
- 家庭用版: https://moyashimisosoup.github.io/shukudai-kanri/
- Git remote: `public-households` = 公開版、`origin` = 家庭用版
- 作業ブランチ: `codex/multi-household-public`（`public-households/main` を追跡）
- 最新公開コミット（この追記前）: `284e907 Let a joining device take the household's design`
- 2026-08-10 時点の配信状況：公開版・家庭用版とも
  app `20260810au` / sync `20260810ac` / style `20260810al` / manifest `20260810y` を配信中（次回公開予定は app `20260810av` / sync `20260810ac` / style `20260810al`）

### 公開版の Pages ビルドが `building` のまま止まったとき

`d7f00d5` の push で、家庭用版は45秒で配信されたのに公開版だけ13分以上
`building` のままになった。エラーは出ていない。**次の push を重ねたら詰まりが解け、
両方いっぺんに配信された。**（下記「現在の公開方式」にある、固まったデプロイが
以降を全てブロックする事故と同じ筋）。

```bash
gh api repos/moyashimisosoup/shukudai-notebook/pages/builds --jq '.[0:3][] | {status, created_at, commit, error}'
```

`status` が `building` で `error` が空なら、待つよりもう1コミット push するほうが早い。
`errored` なら中身の問題なので `error.message` を読むこと。

公開版・家庭用版のコードは同一です。変更は原則として両remoteの `main` へ push してください。

```bash
git push public-households HEAD:main && git push origin HEAD:main
```

## 現在の公開方式（重要）

公開版の GitHub Actions Pages デプロイが、古いデプロイを「進行中」と誤認して失敗しました。そのため公開版は現在、GitHub Pages の `main` ブランチ直配信（legacy）に切り替えています。`.nojekyll` を追加済みです。

- **公開版・家庭用版とも GitHub Pages の `main` ブランチ直配信（legacy）**。push すれば数十秒で公開される
- Actions によるデプロイは使わない。`.github/workflows/pages.yml` は 2026-08-09 に削除した。
  actions/deploy-pages が「前のデプロイが進行中」と誤認して失敗し続け、家庭用版が5世代ぶん
  古いまま配信される事故が起きたため（`f8902b7` のデプロイが固まり、以降を全てブロックした）
- 配信されている版は次で確認できる。ブラウザのキャッシュに惑わされないこと

```bash
curl -s https://moyashimisosoup.github.io/shukudai-notebook/ | grep -o "app.js?v=[0-9a-z]*"
curl -s https://moyashimisosoup.github.io/shukudai-kanri/   | grep -o "app.js?v=[0-9a-z]*"
```

## state に入っているもの（同期する）

`state` は記録の集まりで、`config`（設定）とちがって **id で合流**する。
`config` は「あとに保存した方でまるごと置きかえる」ので、2台で同時に触ると片方が消える。
**複数の端末から書きうるものは config に置かないこと。**

| 欄 | 中身 | 合流のしかた |
|---|---|---|
| `logs` | やったことの記録 | id で合流。`gone`/`trash` の墓標にある id は取りのぞく |
| `books` | 本の記録 | 同上 |
| `progress` | 進みぐあい | 値ごとの時刻で新しい方が勝つ（下記） |
| `trash` | 消した記録の控え（中身つき） | 保護者ページに出す。墓標も兼ねる。最大50件 |
| `gone` | 消した印だけ | 中身を残さない削除に使う。最大300件 |
| `reads` | その日に読んだミニコンテンツ | **logs には入れない**（下記）。最大400件 |
| `messages` | こどもへのメッセージ | 最大3件。新しいものから3件にそろえる |

### 触るときの注意

- **`reads` を `logs` に混ぜない。** 混ぜると「やったこと」が増え、カレンダーが緑になり、
  ごほうびの判定（`didSomethingToday`）まで動く。読んだだけで宿題をしたことにはしない
- **削除は必ず墓標を残す。** `pushTrash()`（中身つき・保護者ページに表示）か
  `pushGone()`（印だけ）を通す。残さないと相手の端末から復活する
- **新しい欄を state に足したら `mergeState()` にも足す。** 足さないと同期で消える
- `messages` は `config.parentMessage` から移行済み。移行は `migrateMessages()` が初回に1度だけ行う

## 進捗バーの構成

- しゅくだいバーは **必須＋任意の合算**。任意をやってもバーが伸びるようにするため。
  バーの中を濃淡で分け、濃い緑＝かならずやる、うすい緑＝つぎにやる
- **間に合うかの判定（いいペース！など）は必須だけ**で行う。任意を多くやるほど
  「よゆう」と出て必須の遅れが隠れるため
- 必須が残っているあいだは「かならず やるは ぜんぶ できた！」系の文言を出さない。
  数が任意のぶんだけなのに必須も終わったように読めてしまう（実際に起きた）
- 残りの数は「ばん」「まい」の合計ではなく**課題の数**で、単位は「しゅるい」

## ミニコンテンツ（`assets/data.js` の `FUN`）

- 配列の末尾が `.map()` になっていて、そこで分野ごとの問いかけ `ask` を解決している。
  **この `.map()` を壊さないこと**。項目に足す値（`lv` など）はオブジェクトリテラルへ直接書く
- `lv` は 2（小2まで）か 3（小3以上）。読める漢字の設定が「漢字のまま」のときだけ
  `lv:3` も出す（`funAllowed()`）
- 配列の添字がそのまま localStorage の既読履歴のキーになる。並べ替え・削除は履歴をずらす

## 同期で実際に起きた事故（同じ轍を踏まないこと）

### 時刻に `|0` を使わない

ミリ秒の時刻は13けたあり、32ビットに入らない。`|0` を通すと負の数に化ける。

```
1786312076482 | 0  →  -394318654
```

時刻の無い側は `0` なので `0 > -394318654` が成立し、**「何も入っていない側」が
「実際に記録された側」に勝つ**。子の端末で記録した段階が親に届かない、という形で
表面化した。両方に時刻があるときは、どちらも同じように化けて大小がたまたま
保たれるため、番号の同期は正常に見えていた（原因が分かりにくい）。

時刻を扱うときは `assets/app.js` の `ms()` を通すこと。比較・並べ替え・端末一覧・
診断表示のすべてで使う。

### 版の新旧を、文字としてならべて決めない

版は `20260810w` → …`z` → `aa` → `ab` と回る。`sort()` でならべると `'w' > 'a'`
なので、**1文字の古い版が2文字の新しい版に勝つ**。`z` を こえた あとは、
いちばん古い端末が「最新」と判定され、実際に最新の端末に（古い）が付き、
更新の案内もあべこべに出る（実機でそうなっていた）。

`newestVer()` を通すこと。日づけ → 通しの長さ → 文字順、の順にくらべる。
形の合わない版が混ざったときは空を返すので、呼ぶ側は
`newest && r.ver !== newest` のように空を除いてから使う。

### 受け取る前に `saveCfg()` を呼ばない（2026-08-10 再発）

下の「起動時に無条件で～」の再発。今度は**初期設定の画面**が入口だった。

```js
config.childName = name;
saveCfg();          // ← ここで configAt が「いま」になる
S.reconnect(code);  // ← つなぐのは そのあと
```

つないだ瞬間に、まだ何も受け取っていない初期値が「いちばん新しい設定」として
グループ全体へ配られ、デザインも題名も課題の並びも消える。受け取る側から見ると
自分の `configAt` のほうが古いので、グループの設定は二度と戻ってこない。

**同じ穴は3つあった。**

1. 初期設定の「はじめる」（`saveCfg()` → `reconnect()` の順）
2. 初期設定の保護者メッセージ選択（`reconnect()` 直後に `saveCfg()`）
3. よそで保存ずみの端末が別のグループに入るとき。`natsu.savedAt.v1` の
   `config` は「この端末がいつ保存したか」でしかなく、**入る先のグループの時刻と
   くらべても意味がない**。古い設定が「新しい」と判定されて配られる

**直しかた（両方いる）**

- `saveCfg()` は、`NatsuSync.awaitingFirstSnapshot()` が true のあいだ
  **手元にだけ書き、時刻を押さず送りもしない**。最初の snapshot を受け取ったら
  ふつうに戻る。グループの文書が無い場合も snapshot は来るので、新規作成は詰まらない
- `reconnect()` の前に必ず `forgetConfigStampForNewHousehold(code)` を呼ぶ。
  前回とちがうあいことばなら `savedAt().config` を落とし、グループの設定が必ず勝つ
  ようにする。**`state` の時刻は落とさないこと**（値ごとに時刻を持って合流する
  ので、落とすと進みぐあいが安全側に倒れる）

### グループの設定を採るかどうかを、時刻の比較だけで決めない（2026-08-10 三度目）

QRの招待リンクから入った端末だけ、デザインが初期値のままになった。記録（`state`）は
届くのに設定（`config`）だけ届かない、という形で出る。**`config` は id で合流できず
「時刻の新しい方をまるごと採る」ので、比較が1回でも狂うと永久に届かない。**

```js
if(remote.config && remote.configAt > (at.config || 0)){   // ← 直す前
```

狂う道は2つある。

1. `remote.configAt` が旧版の `時刻 | 0` で負の数になっていると、`負 > 0` が
   成り立たずグループの設定が**一度も**採られない。`ms()` を通していなかった
2. まだ一度もグループを受け取っていない端末に、よそで押した新しい `at.config` が
   のこっている。`forgetConfigStampForNewHousehold()` は「前回とちがう合言葉」の
   ときしか落とさないので、同じ合言葉で入り直すと落ちない

**直しかた。** つないでから最初の snapshot の1回だけは、時刻を見ずにグループの設定を採る。
まだ受け取っていない端末には「手元のほうが新しい」と言える根拠がない。
`sync.js` の `watchHousehold()` が `first:true` を渡し、`applyRemote()` がそれを見る。
`configAt` / `stateAt` はどちらも `ms()` を通す。グループ側の時刻が壊れていた（0になった）
ときは、採ったあと正しい時刻で送り返してグループの時刻印そのものを直す。

調べるときは `#config` の「デバッグ用：同期の記録」を見ること。`traceConfig()` が
`theme` / `title` / `childName` / `readingGrade` の入れかわりを「こちらの値と時刻／
あちらの値と時刻」の形でのこす。id に「config（つないだ直後）」と出ていれば、
上の初回ルールで採ったということ。

### 起動時に無条件で `saveCfg()` を呼ばない

開いたばかりの端末は、まだおうちの設定を受け取っていない。手元は初期値のまま。
そこで `saveCfg()` を呼ぶと、**初期値が新しい時刻で送られ、設定は「あとに保存した
方が勝つ」ため、全端末のデザインなどが初期化される**。招待リンクで開いた端末が
グループ全体の設定を壊す、という形で表面化した。

移行処理などで設定を書きかえるときは、**実際に書きかえるものがあるときだけ**
`saveCfg()` を呼ぶこと（`migrateMessages()` がその形になっている）。

### 調べ方

`#config` の「デバッグ用：同期の記録」に、合わせたときに入れかわった値が残る。
「こちらの値と時刻／あちらの値と時刻／のこった値」が出るので、どちらがなぜ
勝ったのかが分かる。上の2件とも、これで原因を確定させた。推測で直さないこと。

## 画面まわりで壊しやすいところ

- `bindConfig()` は描き直しのたびに呼ばれる。**`#view` へ直接 addEventListener しない**。
  `#view` は描き直しても要素が残るのでリスナーが積み重なり、1回のタップで何度も動く
  （並べかえの ▲▼ が効かず、iPad では他のボタンまで固まった）。
  一度だけ束ねる `bindConfig._edBound` のガードを外さないこと
- 合わせた結果が変わったかの判定は `sameState()`。`JSON.stringify` の直接比較に戻すと、
  欄の名前の並び順がちがうだけで「変わった」と判定し、端末間の送り合いが止まらなくなる
- グリッドで `1fr` を使うと最小幅が `min-content` になり、折り返さない長い1行があると
  はみ出す（iPhone で実際に起きた）。`minmax(0,1fr)` と `min-width:0` を使う

## 同期のしくみ

- `progress` は値ごとに更新時刻（`doneAt` / `stepsAt` / `wrapAt` / `daysAt`）を持ち、新しい方が勝つ。両方に時刻が無いときだけ従来の max / OR に倒す
- 書きかえは必ず `progPatch()` を通すこと。直接 `state.progress` を代入しない
- 削除は `state.trash`（中身つき・保護者ページに表示）と `state.gone`（印だけ）に墓標を残す。これが無いと相手の端末から復活する
- 変更判定は `sameState()`（キー順をそろえてから比較）。`JSON.stringify` の直接比較に戻すと端末間で送り合いが止まらなくなる
- Firestore の `household` 文書に新しいトップレベルの欄を足すときは、`firestore.rules` の `hasOnly([...])` にも足さないと書き込みが拒否される
- 端末ごとの `ver` は `devices` に入れ、古い版が混ざると訂正が巻き戻る

## 画面の作り（2026-08-09 に変更・要注意）

iPadOS Safari で `position: fixed` の下タブがスクロール中に浮いて途中で止まる不具合があったため、**ページ全体をスクロールさせない作り**に変更しました。ここを知らずに CSS を触ると崩れます。

```
body（縦フレックス・overflow:hidden・height:100%）
 ├ header.topband   … flex:none。sticky ではない
 ├ div#scroll       … flex:1 / overflow-y:auto。★スクロールするのはここだけ
 │   └ main#view    … 中身。app.js が innerHTML で描き替える
 └ nav.tabbar       … flex:none。fixed ではない
```

- スクロール位置の取得・復元は `window.scrollY` ではなく `assets/app.js` の `scrollBox()`（= `#scroll`）を使う
- 上帯・下タブに `position: fixed` / `sticky` を復活させない
- `body` に `padding-bottom` でタブ分の余白を入れる必要はない
- 印刷時のみ `@media print` で3段構造を解除している（解除しないと1ページで切れる）
- 副作用：スクロールしても Safari のアドレスバーが縮まない（ホーム画面追加のアプリ表示では元から影響なし）

## 直近の修正

1. iPad の下タブ固定（`d0a17aa`）
   - 上記「画面の作り」を参照

2. 連続記録の表示（`8bdefbe`）
   - 「1日 れんぞく」という不自然な表記をやめた
   - `assets/app.js` の `streakLabel()` / `streakLabelKanji()` に文言を集約
   - `streakOf()` は「今日がまだでも昨日までを数える」ため、`streak === 1` には
     「今日やった1日目」と「昨日やって今日はまだ」の2状態がある。`p.isDone` で判別する
   - 今日やった1日目は数やハートで達成が見えているのでバッジを出さない。
     「きのう できたね」→ 2日目以降「◯日 れんぞく」

3. ミニコンテンツの整理（`430f021`）
   - 「おもしろいことば」→「ことば」に改称し、デジャヴ／ドッペルゲンガー／
     カプグラ・デリュージョンを追加
   - 長い言葉：「〇〇学でつかう」をやさしい言い方に置換。宇宙ステーション「きぼう」と
     ユネスコを削除、スーパーノヴァ・レムナント・ネビュラと
     ポリテトラフルオロエチレンを追加
   - リュウグウノオトヒメノモトユイノキリハズシに「日本のことばの中でいちばん長いと
     いわれる」を追記
   - 漢字のなりたちに図を追加（次項）

4. 漢字のなりたち図（`430f021`）
   - `assets/kanji-origin.js` を新設。「もとの絵 → むかしの字 → いまの字」の3コマを
     インライン SVG で自前描画（16字）
   - 外部画像を使わないので著作権・オフラインの問題がない。線は `currentColor` なので
     テーマの色に追従する
   - `assets/data.js` のなりたち項目に `fig:'山'` のようなキーがあり、
     `app.js` の `kanjiOriginHTML()` が対応する SVG を差しこむ
   - 図を増やすときは `KANJI_ORIGIN` に `{pic, old}` を足し、data.js に `fig` を付ける

5. 全ミニコンテンツ確認画面
   - URL: `?debug=content#home`（旧 `?debug=trivia` も可）
   - 「OK」は画面から除外、「削除・再検討」は残し、選択内容をコピーしてAIへ渡せる
   - 判定はその端末の localStorage のみ。共有・外部送信はしない
   - `assets/app.js`: `DEBUG_CONTENT` / `contentDebugHTML()` / `K_TRIVIA_REVIEW` 付近

6. 宿題の記録シート
   - 何も選ばず保存しても、`0/6 に変更しました` のログを作らず達成カウンターも増やさない
   - `assets/app.js` の `saveSheet()` に空入力ガードあり

## 実装済みの主な仕様

- 初期設定: 子どもだけで使う / 保護者と共有する を選択。共有時は新規作成なら16文字の共有コードを設定・読み込み（旧版互換として8文字以上も受け付ける）
- 同期: Firebaseを使い、合言葉を知る端末間で設定と記録を共有。端末数表示あり
- 個人データ: 子どもの名前・宿題名・記録は端末/共有用Firebaseに保存。グループ共有はエンドツーエンド暗号化ではないため、Firebaseプロジェクト管理者が保存データを確認できる可能性がある。共有コードに普段のパスワードや秘密を使わない
- 子ども向け表示: 漢字レベルを初期設定・個別設定から変更可能
- 保護者ページ: 漢字表記固定。子どもへのメッセージ、共有接続、進捗サマリー、設定への導線あり
- テーマ: 端末ごとの設定ではなく、共有設定として同期する仕様
- 毎日項目: デフォルトは「おてつだい」。表示可否、単位（かい／ハート／任意）、並び順を設定可能
- 宿題・毎日項目とも上下ボタンで順番変更可能
- 読書は通常宿題と分けた「読書の記録」として扱う（既存データ互換を維持）
- ミニコンテンツ: なぞなぞ、まめちしき、ことば、頭のたいそう、名言、ことわざ、故事成語、
  むかしのことば、漢字のなりたち、めずらしい生きもの、よくわからないけれどかっこいい長い言葉。
  説明を見た後に次へ進める。宿題をした日は追加でもう1件読める

## 主要ファイル

- `index.html`: アセットのキャッシュバージョン、`#scroll` を含む画面の骨組み
- `assets/app.js`: 画面、状態、設定、記録、ミニコンテンツのUI
- `assets/data.js`: デフォルト設定、宿題サンプル、ミニコンテンツ `FUN`
- `assets/kanji-origin.js`: 漢字のなりたち図（インラインSVG）
- `assets/style.css`: レスポンシブUI、テーマ、iPhone/iPad向け調整
- `assets/sync.js`: Firebase同期・端末一覧（役割・呼び名・ver）
- `assets/kanji.js`: 漢字レベルに合わせた表示変換
- `assets/vendor/qrcode.js`: QRコード生成（qrcode-generator 2.0.4・MIT）。
  `index.html` から読みこみ、`app.js` の `inviteQrHTML()` が使う。外部通信なし

## 次に行う場合の注意

- iPhone/iPad の Safari ではキャッシュが強い。`app.js` / `sync.js` / `data.js` / `style.css` / `kanji-origin.js` を
  変更したら `index.html` の `?v=` を更新する（次回公開予定 app `20260810as` /
  sync `20260810aa` / style `20260810ak`）。
  設定画面の「アプリの ver」に、その端末が動かしている版が出る
- `FUN` は配列の添字がそのまま localStorage の既読履歴・確認状態のキーになっている。
  項目を消す・並べ替えると既存端末の履歴が1つずつズレる（壊れはしないが出題順が乱れる）
- 既存グループのデータ互換を守るため、`recordStyle:'book'`、`state.books`、`targetUnit` の
  保存形式は軽率に変更しない
- 設定や記録は同期済みグループにも影響する。削除ではなく非表示・互換処理を優先する
- 上記「画面の作り」の前提（`#scroll` だけがスクロール）を崩さない
- 「はずす」で付けた `devices.<id>.revoked` は、合言葉を明示的に入れ直した
  再登録時に `false` へ戻すこと。見逃し1回だけでは次の同期通知で再び切断される

## 次にやること（2026-08-10 時点の残作業）

1. **実機での確認まち**
   - まねきリンクのQR（下記のとおり組みこみずみ）を、別の端末のカメラで実際に読めるか
   - 読書ゆうびんの同期（負の旧時刻の回復が効いているか）。全端末を `20260810w` 以上にすること
   - 招待リンクでの引きつぎ（LINE → 標準ブラウザ）
   - 端末の「はずす」→ あいことばの再入力が要ること
2. **家庭用版URLを公開版URLへ統合**
   - 同じ Firebase を見ているので、公開版URLで同じあいことばを入れれば記録も設定も出る。
     書き出し・読みこみは不要（保険として書き出しておくのはよい）
   - 移行中は両方のURLが生きているので、同時に開いたままにしない

## Claudeへの検収・判断依頼（2026-08-10、Codex作業引継ぎ）

### 今回公開済みの範囲

- 公開コミット: `d840c2b Improve sync safety and parent controls`。公開版・家庭用版の両方の `main` に push 済みで、配信版は app `20260810ag` / sync `20260810x` / style `20260810ae` を確認済み。
- 旧版の負のミリ秒タイムスタンプを無効化し、読書・進捗の値が「未記録側」に負けないようにした。受信設定の時刻は受信した現在時刻ではなく送信側の `configAt` を保存し、遅れて届く毎日の項目を落とさない。
- 履歴削除の墓標・全削除世代・接続前/失敗時の保留送信を回帰テスト化。メッセージ送信時に端末が再び「解除済み」扱いになる問題も、再登録時に `revoked:false` を書くことで対処済み。
- 新規共有コードは16文字。Firestore文書IDはSHA-256で扱う。既存グループは新IDが空の場合だけ旧IDを試すため、データを分断しない。同期データ本体はエンドツーエンド暗号化ではないことを画面とREADMEに明記した。
- 保護者ページに今日の記録、削除設定への案内、共有状態バッジ、ホーム画面追加案内、設定ページの整理、メッセージ欄のiPad/iPhone調整、子ども画面へ移る確認ダイアログ、毎日の項目6回以上入力などを実装済み。

### QR招待 → ホーム画面追加の最優先修正（未公開）

- 根本原因: `manifest.webmanifest` の `"start_url":"./"` が、QR招待URLの `?join=<共有コード>` を落としてホーム画面版を常にトップURLで起動していた。さらに app.js が通常ブラウザで join を即座に `history.replaceState` で消していた。
- 作業ツリーに未コミットの修正あり: manifest の `start_url` を省略し（既定値は追加したページのURL）、通常ブラウザではホーム画面追加まで `join` を残す。ホーム画面版で最初に開いたときだけ共有コードをlocalStorageへ保存してURLから消す。子ども画面には追加手順と「追加しないのでURLから消す」ボタンを出す。
- キャッシュ版予定: app `20260810ah`、manifest `20260810y`。`tests/sync-regression.test.js` に、通常ブラウザではjoinを残し、standaloneだけURLから除くテストを追加済み。現状 `npm test` は18件すべて成功。
- 実機検収が必要: 既存ホーム画面アイコンはインストール時の起動URLを記憶しているため、一度削除し、QRをSafariで読み込み、この画面のまま「ホーム画面に追加」→新アイコンを起動、で共有が保たれることをiPhone/iPadで確認する。Android/Chromeも同様に確認する。

### Claudeに判断を仰ぎたい点

1. 招待URLには共有コードが含まれる。ホーム画面追加のため通常ブラウザでは一時的にURLへ残す設計とした。追加しない場合の明示的な削除ボタンは設けたが、案内文の安全性・分かりやすさが十分か。
2. 新規コードは16文字・文書IDのハッシュ化まで実施した。データ本体のE2E暗号化は、既存グループの移行・鍵復元・端末追加の設計判断が別途必要なため実施していない。この線引きでよいか。
3. 上記の実機検収で、ホーム画面追加後の同期、オフラインでの記録→再接続、端末解除→再入力、読書ゆうびん/履歴削除の往復同期を優先して確認してほしい。

### Claudeの検収結果（2026-08-10）

**通ったもの**

- `ms()` の `n > 0` ガードは、こちらで独立に特定した根本原因と一致。旧版が保存した
  負のミリ秒は元に戻せないので「時刻なし」に倒し、安全側の合流規則で値を救うのが正しい。
  合流のたびに壊れた時刻が取り除かれるので、移行処理を別に書かなくても自然に直る
- QRは Codex の作業ツリーでも描画を確認（220×220・白地に黒・`role="img"` と `<title>` あり）。
  実運用のURL長（16文字コード）で45モジュール、240px枠で1モジュール約4.2px
- `npm test` 20件すべて成功

**見つけた回帰（修正済み・未公開）**

`join` をURLに残す変更で、**「はずす」が効かなくなっていた**。

1. 親が端末Bを「はずす」→ Bの listener が `setCode('')` して切断
2. BのURLにはまだ `?join=<共有コード>` が残っている
3. Bを開き直すと `applyJoinCode()` が走り、`getCode()` が空なので `reconnect()` する
4. `reconnect()` は `skipRevokeOnce = true` を立て、`registerDevice()` が `revoked:false` を書く
5. **Bはグループに戻り、はずした印まで消える**

ホーム画面版は起動URLそのものに `join` が焼きつくため、**起動のたび**にこれが起きる。
アイコンを消す以外に止める手段がない。以前は `takeJoinCode()` が常にURLから消して
いたので起きなかった。

対処：はずされた時点の合言葉を `natsu.sync.revoked.code.v1` に控え、
`applyJoinCode()` は**その合言葉だけ**自動接続をことわる。人が設定画面で打ち直したら
`forgetRevokedCode()` で忘れるので、「はずす → 入れ直す」は従来どおり動く。
同じ状態では「ホーム画面に追加」の案内も出さない（つながらないのに
「引き継げる準備ができています」と出るのはうそになるため）。
`tests/sync-regression.test.js` に2件追加。

**あわせて入れたもの**

- `index.html` に `<meta name="referrer" content="strict-origin-when-cross-origin">`。
  URLに合言葉が載っているあいだ、Google Fonts などへアドレスを渡さない。
  いまのブラウザの既定でも同じだが、既定の緩い古い端末で漏れるのを塞ぐ
- キャッシュ版：app `20260810ai` / sync `20260810y`

**判断依頼への回答**

1. **案内文より、消えない置き場所のほうが問題。** Android は manifest の `start_url` が
   省略されると追加時のURLを起動URLとして記憶するので、合言葉がOSのアプリ登録に
   残りつづける。上の回帰対処でこれ以上の実害は塞いだが、「消すボタン」は
   ホーム画面追加後には届かない点は残る。文言としては「共有コード＝あいことばそのもの」
   だと明記したほうがよい（いまは「共有コード」としか書いていない）
2. **この線引きでよい。** E2E暗号化は鍵の受け渡しがQR招待とほぼ同じ問題に帰着し、
   鍵を失うと全グループのデータが復元不能になる。いまは「Firebase管理者が見られる」ことを
   画面とREADMEに明記する形で足りている。先に済ませるべきは、合言葉が
   URL・履歴・OSのアプリ登録に残る経路を減らすこと
3. 実機確認は未実施（こちらからは実機を触れない）。**「はずす → 開き直す」を
   最優先で確認してほしい。** 上の回帰はまさにその手順で出る

## Codex継続分（2026-08-10・未コミット）

Claudeが設定ページの該当箇所へ移動する処理を作業中に停止したため、差分を監査して
そのまま引き継いだ。`pendingJump` / `jumpTo()` / `jumpToSection()` は、ページ全体ではなく
`#scroll` を動かし、描画直後のレイアウト確定を待って対象を上端へ寄せる。保護者ページの
共有バッジは `#syncSection`、今日の記録の案内は `#logCareSection` へ移動する。

同じ未コミット差分へ、次を追加した。

- 保護者ページの未送信表示を「送信後、ここに表示されます。」へ短くし、狭幅でも
  1行に収まるようにした
- 子ども画面の保護者メッセージを1つの外枠へまとめ、複数件は点線で区切った
- 561px以上では送信ボタンをメッセージ入力欄と同じ段の右端へ置き、スマートフォンでは
  入力欄の下の全幅ボタンへ戻す
- 「記録の手入れ」の内側に左右余白を設けた
- 長い共有上の注記を「注意事項／内容を確認」にまとめ、クリック時に一般的な注意を表示する。
  合言葉を手入力して接続するときも同じ内容を確認してから進む
- `watchHousehold()` は、空のFirestoreキャッシュを「グループ設定を受信済み」と数えない。
  QR参加直後に空キャッシュが先に届いても、その端末の初期デザインをグループへ送れないため、
  `9e1dbbe` の送信保留と合わせてデザイン初期化の競合を塞ぐ
- 公開予定のキャッシュ版は app `20260810am` / sync `20260810aa` /
  style `20260810ag` / manifest `20260810y`

`node --check assets/app.js`、`node --check assets/sync.js`、`npm test`、`git diff --check` は成功。
回帰テストは27件。ローカル表示では320px幅に横スクロールがなく、未送信表示も
`scrollWidth === clientWidth`。768px幅では本文欄と送信ボタンの下端が一致した。
320px幅で2件の子ども向けメッセージが1外枠・1本の点線区切りになること、記録の手入れへ
移動後の見出しが`#scroll`上端から約12pxに収まり、内枠の左右余白が16pxになることも確認した。
現時点ではコミット・公開していない。

QR参加の実機検収は次の順で行う。保護者端末でデフォルト以外のテーマ、子どもの名前、
課題を設定し、未接続のiPhone/iPadでQRを読む。ブラウザ上でそれらが反映されたことを
確認してから「ホーム画面に追加」し、追加したアイコンから開いても接続済み・同じデザイン
であることを確認する。最後に保護者端末で設定を1つ変え、ホーム画面版へ同期することを
確認する。古いmanifestで追加済みのアイコンは起動URLが固定されている可能性があるため、
検収時は一度削除して新しく追加する。

Claudeには、上の競合対策が新規グループ作成（文書がまだ無い場合）を止めないこと、注意事項の
表現がグループ向けとして十分か、320 / 375 / 768 / 1024pxでメッセージ欄が崩れないことを
重点的に検収してほしい。

## 2026-08-10 追記（実機の指摘への対応・次回公開予定）

公開予定のキャッシュ版は app `20260810at` / sync `20260810ab` / style `20260810al`。
回帰テストは41件。

1. **QR参加でデザインが初期値に戻る**。上の「グループの設定を採るかどうかを、時刻の
   比較だけで決めない」を参照。**実機での再確認が要る。** 直っていない場合は、
   `#config` の同期の記録に config の行が出るので、それを見てから直すこと
2. **新規作成の合言葉**は自動作成のまま読み取り専用。「自分で決めた合言葉を使う」を
   押した人だけ手入力になる。手入力にすると注意書きが出る。
   おためしモードだから編集できないのではなく、これが仕様
3. **名前・漢字の設定は、すでにあるグループへ入るときは任意**。空のまま進めても
   `config.childName` / `config.readingGrade` を書きかえず、グループの設定がそのまま届く。
   判定は `joining = sharing && (role === 'child' || !creating)`
4. **QR招待の「ホーム画面に追加」案内**（`joinInstallTransferHTML()`）に
   `data-no-reading` を付けた。子ども画面に出るが読むのは大人なので、
   `applyReadingDisplay()` のかな変換から外してある。**この属性を外さないこと。**
   外すと「きょうゆうコード」「ホームがめんばん」になり、大人が読めない
5. **端末一覧に役割を出す**。呼び名（父・iPad など）とは別に「親」「子」「未設定」の
   バッジを出す。`deviceRows()` が `roleLabel` / `roleShown` を返す。
   呼び名が「親」そのものなら重ねない。
   呼び名が空のときは `deviceKindLabel()` の「iPhone」「iPad」などを既定にして送る。
   **ブラウザは機種名（iPhone SE など）までは通知しない**ので、これ以上は細かくできない

## 未着手の宿題（2026-08-10 実機で出た指摘）

1. **ホーム画面アイコン**。`manifest.webmanifest` の `assets/paw.svg`（肉球）が
   そのままアイコンになる。iOS 16.4 以降は Safari も manifest の `icons` を読むので
   iPhone でも出る。**利用者は肉球を望んでいない。** もっと簡素な図案に差しかえる。
   `apple-touch-icon` は未設定なので、iOS 向けには 180×180 の**不透明** PNG を
   足すのが確実（透過だと iOS が黒で埋める。`paw.svg` は `fill="#000"` の透過）
2. **「今日の記録」の名前表示**。いまは全件に「（はじめ）」が付く。
   保護者が入れたものだけ「（親）」と出す

（ホーム画面追加の案内のはみ出し・かな変換、読んだミニコンテンツの表示は対応ずみ。
上の「2026-08-10 追記」と、`viewLog()` の `readsHTML()` を参照）

## 確認コマンド例

```bash
node --check assets/app.js
node --check assets/data.js
node --check assets/kanji-origin.js
npm test
git status --short --branch
```

ブラウザで見るときは、静的サイトなのでローカルの簡易HTTPサーバで `index.html` を開けば動きます。
初期設定を飛ばしたいときは、`localStorage` に `natsu.onboarding.v1 = 'done'` と
`natsu.device.name.v1` を入れてから再読み込みしてください。

初期設定の表示確認には、公開URLの末尾に次を付ける。どちらも preview 専用の
localStorageだけを使い、普段のグループデータ・合言葉・Firebaseには触れない。

- `?new=1&debug=welcome-child#welcome` … 子ども端末の「あいことばを入れる」画面
- `?new=1&debug=welcome-parent#welcome` … 保護者端末の「あいことばを作る」画面

2026-08-10追記：初期設定の使い方・端末役割は、未選択時は同じ白地、選択後は緑＋チェックで状態を保持する。共有を選んだ保護者側では、画面を開く前に自動作成した合言葉のQR・招待リンク・接続手順を表示する。子ども側では、合言葉または保護者から受け取ったQR／招待リンクで接続する流れを最終ボタンの前に案内する。

2026-08-10追記：初期設定は①ホーム画面への追加、②使い方の選択に続き、選択したルートごとに③以降を連番表示する。子ども画面へ進むルートだけ、後続設定の先頭に「どの色が すき？」を表示し、6種類から選ぶと即時プレビューする。共有へ参加する子どもの選択は `natsu.welcome.theme.v1` に一時保持し、グループ設定の初回受信後にデザイン1項目だけを保存する。受信前の初期設定一式をグループへ送らないこと。

2026-08-10追記（次回公開予定）：共有する初期設定を端末の役割とグループの状態で分岐した。
子ども端末は合言葉を入力するだけで、作成・作成者向け注意・「父、母」の例を出さない。
保護者端末は「新しく作る／今あるグループに参加」を選び、新規作成後はさらに「今つなぐ／
あとでつなぐ」を選ぶ。「今つなぐ」にだけQR・招待リンク・既存子ども端末でタイトルを
5回タップする手順を出す。未共有の保護者ページには「共有なし：接続設定はこちら」、
設定のデザイン欄には子ども端末にも反映される旨を表示する。

音声入力は、古い認識セッションの `onend` が新しいセッションを消し得る状態と、エラー時に
`sr` を解放しない状態を修正した。権限・マイク・通信・無音を区別し、失敗後も再開できる。
Webアプリからマイクを「常に許可」へ強制変更はできないため、毎回確認される場合はSafariの
Webサイト設定でマイクを「許可」にする案内を出す。回帰テストは35件。

## 2026-08-10 Codex追記（参加前確認・曜日読み・次回引継ぎ）

### 今回実装したもの

- `284e907` は `public-households/main` と `origin/main` の両方へ公開済み。
- 既存グループへ参加する保護者・子どもの初期設定では、合言葉入力だけでは参加ボタンを出さない。
  `assets/sync.js` の `verifyHousehold()` が新方式ID、次に旧方式IDをFirestoreから**読み取りだけ**で
  確認する。存在しない合言葉で新しいグループを作ったり、`devices` を登録したりしない。
- 確認中は「接続しています…」、確認成功後は「接続しました ✓」を表示し、その後にだけ
  「このグループに参加する」を表示する。入力を変更したら確認状態と参加ボタンを取り消す。
- 接続先の `config.childName` と `config.readingGrade` をフォームに表示する。名前が設定済みなら
  「お子さんの名前・漢字の扱いに変更がなければ、そのまま…」と案内し、未設定なら
  「お子さんの名前は未設定です（任意入力）。以下から設定できます。」と案内する。
- 参加画面で名前・漢字設定を変更した場合は `K_WELCOME_JOIN` に一時保存し、グループのconfigを
  初回受信した**あと**で1回だけ反映する。確認前の端末初期値でグループを上書きしない。
- `?new=1` のおためし画面は実Firebaseを読まず、preview設定で接続成功時の表示を確認できる。
- 子ども画面のかな変換前に `（月）` など括弧内の曜日だけを `（げつ）` などへ確定する。
  月日の「8月」の読みには触れない。
- 端末の呼び名にあった「ほかの端末の一覧にも表示されますが、変更できるのはこの端末だけです。」
  は、初期設定・設定画面の両方から削除した。
- 配信版は app `20260810au` / sync `20260810ac` / style `20260810al` / manifest `20260810y`。
- 回帰テスト45件、`node --check`（app/sync/tests）、`git diff --check` は成功。
  ローカルの320px幅で保護者・子ども両経路に横はみ出しなし。確認前ボタン非表示、短い
  合言葉の停止、確認後の状態・ボタン・未設定名案内、コンソールエラーなしを確認した。

### 実機で確認が必要なもの

1. 公開後、実在するグループの合言葉で保護者・子どもの両参加経路を試す。接続先の名前・漢字設定が
   表示され、変更なしならグループconfigが変わらず、変更ありなら初回受信後にその項目だけ変わること。
2. `284e907` のQR参加修正を、デフォルト以外のテーマ・名前・課題があるグループと未接続iPhone/iPadで
   再検収する。ホーム画面追加前後と、追加後の設定変更同期まで確認する。
3. 曜日表示を「すべてひらがな」で確認し、月曜日が「げつ」、ほかの曜日も
   「にち・か・すい・もく・きん・ど」と表示されること。

### 未着手・重要未確認

1. **ホーム画面アイコン**：`assets/paw.svg` の肉球を簡素な図案へ差し替え、iOS用に
   180×180の不透明PNG（`apple-touch-icon`）を追加する。
2. **「今日の記録」の入力元表示**：現状の「（はじめ）」問題を調べ、保護者が入れた記録だけ
   「（親）」と表示する。既存データ互換と入力元の保存方法から設計する。
3. **一般公開時のライセンスを最終確認**：現状はコード=`LICENSE`（Apache-2.0）、画面文・
   ミニコンテンツ・図=`LICENSE-CONTENT.md`（CC BY 4.0）、第三者通知=`NOTICE` と各vendor内表記、
   画面内クレジット、READMEの再配布条件まで実装済み。ただし、全素材の出自・第三者ライセンス
   の網羅性と、画面内表示が実際の配布形態で十分かは法的レビューをしていない。一般公開前の
   最終チェック項目として残す。
4. **Firebase管理者にデータが見える点の方針を最終確認**：現状はE2E暗号化なし。Firebase
   プロジェクト管理者は名前・宿題・記録を確認できる可能性がある。対策済みなのは、16文字の
   ランダム合言葉推奨、文書IDのSHA-256化、一覧取得禁止、匿名認証必須、既知フィールド以外の
   書込み禁止、URL漏えい対策、画面とREADMEの注意喚起。以前の判断記録では「現状の注意喚起で
   足りる」としているが、依頼者は未完遂と認識しているため、(a)現状維持、(b)E2E暗号化、
   (c)別の認可方式、のどれを採るか明示的に再決定する。また、`firestore.rules` は記録用であり、
   Firebase Console上の実ルールが一致しているかは別途確認が必要。

### Claudeへの次回依頼文

`CLAUDE_HANDOFF.md` を最初から読み、特に末尾の「2026-08-10 Codex追記」を基準に作業してください。
公開済みコードの実機検収結果があれば最優先で反映してください。次に、(1)ホーム画面アイコンの
肉球差し替えとiOS用不透明180px PNG、(2)「今日の記録」で保護者入力だけ「（親）」と出すための
入力元設計・既存データ互換、を実装・検証してください。同時に、一般公開時のライセンス網羅性と
Firebase管理者が平文データを見られる現状について、既存対策を監査し、現状維持/E2E暗号化/別認可
の選択肢・移行コスト・利用者への影響を整理して、依頼者が明示決定できる提案を出してください。
合言葉や個人情報はリポジトリ・報告へ書かないでください。変更後はキャッシュ版を更新し、
`node --check assets/app.js`、`node --check assets/sync.js`、`npm test`、`git diff --check`、
320/375/768/1024pxの表示を確認してから、公開許可がある場合だけ両remoteのmainへpushしてください。

## 2026-08-10 Codex追記2（招待URLで参加端末だけ初期デザインになる件）

実機で、招待URLから入ったブラウザだけグループと違う初期デザインになる症状が継続した。
コード上で次の2経路を修正した。

1. `K_WELCOME_THEME` は、共有参加の途中で子どもが選んだデザインを初回config受信後に重ねる
   一時値だった。接続失敗などで残ると、後日べつの招待URLで入った場合にも古い端末色を
   グループconfigの上へ重ね得た。今後は `{code, theme}` として保存し、確認済みの同じグループだけに
   適用する。旧版の文字列値・別グループの値は削除して採らない。`applyJoinCode()` は招待接続前に
   この一時値を必ず削除する。
2. デザイン共有機能より前からあるグループは、Firestore上の `config.theme` が無く、各端末の
   `K_THEME` だけで色を持っている場合がある。既存端末がそのグループを開いたときは、実際に
   使ってきたローカル色を共有configへ1回移行する。一方、招待URLで入った端末は初期色しか
   知らないため移行元にしない。検収前に、招待を出す既存端末側も最新版へ再読み込みすること。

手動の初期設定参加でも、合言葉確認後にグループのthemeを色選択へ反映する。変更しなければ
一時デザインを作らず、グループの色をそのまま採る。

ホーム画面追加案内から、URLの合言葉が自動削除される旨の説明文を削除した。ボタンは
「追加しない」だけにした。削除処理そのもの（`#joinRemove` とホーム画面版の自動処理）は残す。

配信予定は app `20260810av` / sync `20260810ac` / style `20260810al`。
回帰テストは46件。実機では、既存端末を最新版へ再読み込みしてグループthemeの移行を済ませ、
新しい招待URLを別ブラウザで開いてグループと同じデザインになることを再確認する。

## 2026-08-10 Claude追記（アイコン・入力元表示・ライセンス／Firebase監査）

配信予定のキャッシュ版は app `20260810aw` / sync `20260810ac` / style `20260810al` /
manifest `20260810z`。回帰テストは50件。`node --check`（app / sync / codex-icons /
tools/make-icons）、`npm test`、`git diff --check` は成功。
320 / 375 / 768 / 1024px で `documentElement`・`#scroll`・今日の記録の一覧すべてに
横はみ出しなし、コンソールエラーなし。

### 前提（実機検収について）

依頼時点の指示にあった6項目（合言葉確認前は参加ボタンを出さない、接続先の名前・漢字表示、
変更なしならグループconfigを変えない、QR参加でのテーマ・名前・課題の引きつぎ、曜日のかな）は、
**依頼者から不具合の報告を受けていない**。招待URLのデザイン問題は `0ed16ef` で公開ずみ
（app `20260810av`）。この追記では、その上に未着手分だけを積んでいる。

### 1. ホーム画面アイコン（実装ずみ）

- `assets/icon.svg` を新設。紺地に紙・罫線2本・みどりのチェックだけの図案。
  **`assets/paw.svg` は消していない。** 肉球は「ねこ」テーマの飾りとして
  `style.css` の `mask:url("paw.svg")` から4か所で使われており、消すとテーマが崩れる。
  アイコン用途から外しただけ
- `tools/make-icons.js` が `icon.svg` と同じ座標から PNG を作る。画像ライブラリを
  入れずに済ませたのは、依存を増やすと NOTICE に書く第三者ライセンスも増えるため。
  Node 同梱の `zlib` だけで PNG を書き出している。図案を直すときは **SVG と
  このスクリプトの両方**の数字をそろえ、`node tools/make-icons.js` で作り直すこと
- 出力は `icon-180.png`（iOS の apple-touch-icon）/ `icon-192.png` / `icon-512.png` /
  `icon-maskable-512.png`。すべて**アルファ無し（不透明）の truecolor PNG**。
  iOS は透過を黒で埋めるので、ここを透過に戻さないこと
- `index.html` に `<link rel="apple-touch-icon">` を追加。iOS は manifest の
  `icons` より apple-touch-icon を優先する
- maskable だけ図案を 0.76 倍にして、まるく切られても中身が欠けないようにしてある
- 回帰テストで、manifest が `paw.svg` を指していないこと・実体があること・
  apple-touch-icon が 180×180 でアルファ無しであることを機械的に見ている
- **実機で確認が要る**：iPhone / iPad は追加ずみのアイコンを更新しない。
  一度削除してから追加し直すこと。Android も同様

### 2.「今日の記録」の入力元表示（実装ずみ）

原因は保存側ではなく**表示側**だった。記録には作成時から `l.by`（その端末の役割）が
入っており、`logByLabel()` が `by:'child'` を `config.childName` に読みかえていた。
子の端末で入れた記録が普通なので、結果として今日の記録の全件に子どもの名前が並んでいた。

- `logByLabel()` は `by === 'parent'` のときだけ `親` を返す。ほかは空
- 「消した記録」の控えも、`r.by` の有無ではなく**出す文字があるか**で判定するよう変更。
  以前の条件だと子どもの記録に空の `<span>` が残った
- **保存形式は変えていない。** `by` は記録作成時から `logs` / `books` / `trash` /
  `messages` に入って id で合流しており、追加の移行処理は要らない。
  `by` の無い古い記録は印が出ないだけ
- `logBy()` は共有中だけ `by` を記録する（`sharingOn()`）。共有していないグループは端末が
  1台で見分ける意味がないため。ここを外すと、単独利用の保護者端末で全件に「（親）」が
  付くようになるので、外す前に役割の初期値を確認すること

### 3. ライセンス監査（A）

**見つけた欠落と、その場で直したもの**

`NOTICE` に、同梱している第三者の著作物のうち3件が載っていなかった。Apache-2.0 は
再配布時に NOTICE を引き継ぐことを求めるので、抜けたまま公開すると再配布した人まで
表示を欠くことになる。次を追記した。

- `assets/kuromoji.js`（Apache-2.0 / Takuya Asano）。browserify で束ねられており、
  同ファイル内に async（MIT）などの依存ライブラリと Atilika Inc. の著作権表示が含まれる
- `assets/dict/*.dat.gz`。kuromoji 付属の辞書で **IPADIC（mecab-ipadic）由来**
- `@codexteam/icons`（MIT / CodeX）。`assets/vendor/codex-icons-LICENSE.txt` に全文はあったが
  NOTICE から参照されていなかった
- 実行時に外部から読み込むだけで再配布していないもの（Firebase SDK・Google Fonts の
  Zen Maru Gothic）も、区別して明記した
- `assets/codex-icons.js` の先頭が `SPDX-License-Identifier: Apache-2.0` だけになっており、
  中身の SVG が MIT であることと食いちがっていた。`Apache-2.0 AND MIT` に直し、
  どちらがどの部分かをコメントで書いた
- 新しいアイコンは自作なので `NOTICE` に自作として明記。README のファイル構成も、
  実在しない `.github/workflows/pages.yml` を消し、辞書・アイコン・tools を足した

**依頼者が決める／確認することとして残るもの（法的な断定はしない）**

1. **IPADIC の配布条件の原文確認。** 辞書は `.dat.gz` のバイナリで、リポジトリ内に
   条文がない。上流（kuromoji / mecab-ipadic）の配布物から条文を取り、必要なら
   `assets/dict/LICENSE` として同梱するのが確実。**一般公開前にこれだけは
   原文にあたることを勧める。** 辞書は17MBあり、同梱している以上「再配布」にあたる
2. **ミニコンテンツの出典。** `assets/data.js` の名言・ことわざ・故事成語・古語は、
   `LICENSE-CONTENT.md` で「原典はそれぞれの作者・出典に帰属」と書いてある。
   近現代の作家の言葉（原文が保護期間内のもの）が混ざっていないかは、
   人が一覧を見て判断するしかない。網羅確認は未実施
3. **画面内クレジットの十分性。** `creditHTML()` は作品名・著作者・年・
   ライセンスへのリンク・版を、保護者ページと設定の末尾に出す。子ども画面には出さない。
   CC BY 4.0 は「利用する形態に応じた合理的な方法」を求めるので、
   子ども画面にも出すかは判断の余地がある。いまの置き場所は妥当と考えるが断定はしない
4. **改変者への案内。** いまの文面は「作者が公開している状態」を前提にしている。
   丸ごとコピーして公開した人が、`CREDIT` を自分の名前に書きかえてしまうと
   CC BY の表示義務を満たさなくなる。README にその注意を1行足すか検討

### 4. Firebase のデータ保護（B）

**現状の確認結果**（コードを読んで裏を取ったもの）

- 新規グループの文書IDは合言葉の SHA-256。`list` は全コレクションで禁止、`delete` も禁止。
  匿名認証必須、`households` は `config` / `state` / `configAt` / `stateAt` / `devices` の
  5欄だけ許可。合言葉は16文字・31種の文字から乱数生成（約79ビット相当）
- **旧方式のグループは、文書IDが合言葉そのもの**（`legacyHouseIdFor()` は英数16文字以上の
  合言葉を小文字化してそのまま返す）。既存グループを切らないために残してある経路だが、
  そのグループでは Firebase Console 上に合言葉が文字列として並んでいることになる
- **`devices.<id>.name` に子どもの名前が入る**（`deviceInfo()`）。暗号化を検討するなら
  `config` / `state` だけでは足りない
- 合言葉を手入力にしたグループは、SHA-256 が無塩なので辞書攻撃に弱い。自動生成の合言葉なら
  問題にならない。画面が自動生成を既定にしているのは正しい
- `households` の `delete` が禁止なので、**グループが自分でデータを消す手段がない**。
  「やめるときにデータを消したい」という求めに応じられるのは管理者だけ
- `metrics/registrations` は誰でも +1 できる。数字の水増しは可能（実害は小さい）

**3案の比較**

| | (a) 現状維持 | (b) E2E暗号化 | (c) 別の認可方式 |
|---|---|---|---|
| 管理者が中身を読めるか | 読める | 読めない | **読める（解決しない）** |
| 移行コスト | なし | 中。sync.js に暗号層、移行処理、テスト | 大。アカウント基盤とルールの作り直し |
| 既存グループとの互換 | — | **いちばん危ない所**。旧版端末は暗号文を読めず「設定が空」に見え、上書き事故になりうる。版で門番を付ける必要がある | 全グループの作り直しに近い |
| 鍵（合言葉）の紛失 | 管理者に頼めば救える余地がある | **完全に復元不能** | アカウント復旧はできる |
| 端末追加 | いまと同じ | いまと同じ（鍵＝合言葉なので配布経路が増えない） | 子ども端末にもアカウントが要る |
| 利用者への影響 | 変わらない | ほぼ変わらない（初回の再暗号化のみ） | 大きい。小学生にアカウント作成を課す |

補足を2つ。

- **(c) はこの懸念を解決しない。** 認可を強くしても、平文で保存されている以上
  プロジェクト管理者は読める。(c) を選ぶ理由は「合言葉の共有をやめたい」ときだけで、
  いまの課題（管理者に見える）とは別の話
- **(b) の鍵配布は新しい問題を作らない。** 鍵は合言葉から導出すればよく、
  合言葉はすでにQR・招待リンクで配っている。以前「鍵の受け渡しがQR招待と同じ問題に
  帰着する」と書いたのはその通りで、**つまり追加の困難がない**という意味でもある。
  本当の障壁は鍵配布ではなく、上の表の「既存グループとの互換」の1行

**この監査からの推奨**

いますぐ (b) に踏み切る必要はない。ただし (a) のままなら、次の3つは (b) より安く、
効き目が確実なので先に片づけることを勧める。

1. **旧方式ID（合言葉が文書IDになっているグループ）の付け替え。** 既存端末が接続したときに、
   ハッシュIDへ1回コピーして以後そちらを使う。旧文書は `delete` が禁止なので
   管理者の手作業で消す。これは「合言葉が管理者から丸見え」という一番強い漏えい経路を消す
2. **グループが自分でデータを消せる道を用意する。** いまは `delete: if false` で、
   利用者からの削除要求に応える手段が管理者の手作業しかない
3. **合言葉の手入力を選んだグループへの注意を強める。** 無塩SHA-256なので、
   弱い合言葉は総当たりに耐えない

**`firestore.rules` と Console の一致確認（未実施・要依頼者）**

このファイルは控えで、実際に効くのは Console に貼った内容。次のどちらかで確かめる。

```bash
npx firebase-tools firestore:rules get
```

取得できない場合は Console（Firestore Database → ルール）を開き、`firestore.rules` と
1行ずつ見くらべる。**特に `allow list: if false` が全コレクションに効いているか**を
最優先で見ること。ここが緩んでいると、ほかの対策がすべて無効になる。
一致していたら、その旨をこのメモに追記して以後の確認を省けるようにする。

### 次に触る人への注意

- アイコンの図案を直すときは `assets/icon.svg` と `tools/make-icons.js` の**両方**。
  PNG を手で描き直さない
- `logByLabel()` に子どもの名前を戻さない。今日の記録が名前で埋まる
- `paw.svg` を消さない。「ねこ」テーマの飾り

## 2026-08-11 Claude追記（QR参加でグループの設定が消える件・共有UIの整理）

配信予定のキャッシュ版は app `20260811a` / sync `20260811a` / style `20260811a` /
manifest `20260810z`。回帰テストは55件。

### 1. QR参加するとグループの設定が消える（根本原因を1つ特定・修正）

実機で「参加した端末だけでなく、招いた元の端末からも まいにちの項目が消えた」。
`assets/sync.js` の `watchHousehold()` に、次の欠陥があった。

```js
if(mayUseLegacy && snap.metadata.fromCache) return;   // ← 直す前
...
registerDevice();
pushAll();          // まだグループを受け取っていない端末の初期値をグループへ書く
```

すぐ上のコメントは「キャッシュだけでは新旧を決めない」と書いてあるのに、判定に
`mayUseLegacy` が混ざっていた。**旧方式のグループへ切りかえた あとの watcher は
`mayUseLegacy = false` で動く**ので、そこだけ素通りする。招待リンクで入ったばかりの
端末はその参照をまだためていないため「文書なし（キャッシュ）」が1回来る。そこで
`pushAll()` が走ると、**受け取る前の初期設定がグループぜんたいの中身になる**。

引き継ぎメモの「起動時に無条件で `saveCfg()` を呼ばない」と同じ事故が、app.js 側では
塞がれていたのに sync.js 側に残っていた形。`configHeldBack()` は app.js の保存経路しか
守れず、`pushAll()` はそこを通らない。

**直しかた（2段構え）**

1. キャッシュ判定から `mayUseLegacy` を外す。オンラインで「無い」と確認できた
   ときだけ先へ進む
2. `reconnect(code, { joining:true })` を足した。招待リンク（`applyJoinCode()`）と
   初期設定の参加は「あるグループへ入る」ので、**グループが見つからなくても新しく作らない**。
   静かに上書きせず「この合言葉のグループが見つかりません」と画面に出す。
   グループを1回受け取ったら `joiningExisting` はおろす

**デザインが移行先で反映されない件も、同じ原因の可能性が高い。** 参加端末の初期値
（theme は既定の notebook）がグループへ配られれば、グループのデザインが初期値に変わる。
`pushAll()` が走るかどうかはキャッシュの状態しだいなので、実機で「反映されるときと
されないときがある」という形になる。**ただし断定はしていない。** 実機で再確認すること。

### 2. 同期の記録に、まいにちの項目と作成の瞬間を残した

原因の切り分けに時間がかかったのは、`traceConfig()` が `theme` / `title` /
`childName` / `readingGrade` の4つしか見ていなかったため。次を足した。

- `showDaily` を追跡対象に追加
- `taskCensus()` で「tasks（数）」「まいにち（数）」を追跡。**課題名そのものは
  残さない**（保護者ページに出るため、個人情報を増やさない）
- `NatsuApp.onHouseholdCreate()` を新設。sync.js がグループの文書を**新しく作る**
  ただ1つの道で呼ばれ、「グループを新しく作った」という行を記録に残す。
  意図せず起きたときに `#config` の「デバッグ用：同期の記録」で気づける

再発したら、まずこの記録を見ること。推測で直さない。

### 3. 設定画面の共有を「作成＝共有開始」にそろえた

「作成する」と「この合言葉で接続」が別操作で、どちらを押した時点でほかの端末から
読めるのかが読み取れなかった。未共有のときは2つの入口に分けた。

- **はじめて共有する**：「合言葉を作成する」を押した時点で作成・接続まで済ませ、
  QR・招待リンクを開いて見せる。注意事項の確認はこの1回に前倒し
- **ほかの端末で作った合言葉に参加する**：「接続を確認」→「接続しました ✓」→
  「このグループに参加する」。初期設定の参加と同じ流れ。確認前は参加ボタンを出さず、
  `reconnect(c, { joining:true })` でグループを作らせない

すでに共有ずみの画面は従来どおり（合言葉欄＋「この合言葉で接続」）。

### 4.「項目を初期状態に戻す」を「宿題の項目をすべて消す」に変更

`freshConfig()` でサンプルの宿題一式が復活していた。消したのに知らない宿題が並ぶので、
`config.tasks = []` / `config.showDaily = false` にして空にする。名前・デザイン・期間の
設定と記録は残す。課題が0件のときの子ども画面は `homeEmptyHTML()` に差しかえた。
空のまま `sectionHTML('must', …)` を出すと「ぜんぶ できた！」になり、**何も登録して
いないのに終わったように見える**（ペースの計算も分母が無い）。大人あての案内には
`data-no-reading` を付けてある。**この属性を外さないこと**（かなになって読めなくなる）。

### 次にやること

1. **実機で 1 の再確認。** 招待を出す端末を最新版へ読み直してから、別ブラウザで
   招待URLを開く。デザイン・まいにちの項目がグループのまま届くこと。届かない場合は
   `#config` の同期の記録に「グループを新しく作った」の行が出ていないかを見る
2. 旧方式のグループ（文書IDが合言葉そのもの）の付け替え。依頼者のグループのみ該当で、
   手動移行の方針。**既存グループはもう無いものとして設計してよい**との判断が出ている
3. E2E暗号化の可否。既存グループが無いなら移行リスクが消えるので、前回の比較表のうち
   「既存グループとの互換」の行が丸ごと落ちる。判断しなおす価値がある

---

## 2026-08-11 Claude追記2（E2E暗号化・3ページ化・グループ呼称・スピンオフ）

### 配信状況

`1f72f17` を公開版・家庭用版の両方へ push 済み。
`index.html` の版：app / style / tokens = `20260811q`、sync = `20260811d`、data = `20260811c`。
テスト 90件 通過。

**リリース前に必ず：Firebase Console のルールを貼り直すこと。**
`firestore.rules` はこのセッションで2点変えた。**貼るまで効かない。**

- `validHouseId` を `[0-9a-f]{64}` のみに（旧方式IDの新規作成を止める）
- `sealedContent()` を追加。`config`/`state` が**文字列（＝暗号文）でなければ書き込み拒否**

手順：Firebase Console → 構築 → Firestore Database → ルール → 全選択して削除 →
`firestore.rules` を全文貼り付け → **公開**。

### この回で入れた大きな変更

**1. 中身の E2E 暗号化（`assets/sync.js`）**

名前・宿題・記録は端末で AES-GCM をかけてから Firestore へ送る。鍵は合言葉から
PBKDF2（SHA-256・250,000回）で導き、どこへも送らない。Firebase の管理者でも読めない。

- **塩は文書IDでも合言葉そのものでもない。** 文書IDは既に `SHA-256(合言葉)` なので、
  それを塩にすると「保存された値」と「鍵の材料」が同じになる。
  `SHA-256('natsu.e2ee.salt.v1|' + 正規化した合言葉)` を使う
- 欄の名前を AAD に入れてある。`config` の中身を `state` の欄へ移されても復号に失敗する
- **時刻（`configAt`/`stateAt`）は平文のまま。** 新旧の判定に使う。隠すと復号しないと
  比較できず、鍵のない端末が「文書はあるのに何も分からない」状態になる
- `devices.<id>` は `name`/`label` だけ `enc` に封じる。`role`/`ver`/`revoked`/`at` は
  平文（復号の前に使うため。はずされた判定・台数・版ちがいの注意）
- 鍵の導出は実測 128ms（初回のみ、以後キャッシュ）

**復号できないときの決まり（ここを崩さないこと）**

```
gotSnapshot を立てない／joiningExisting を落とさない／onRemote を呼ばない／
pushAll・flushPendingSoon を呼ばない → そのまま return
```

先に `gotSnapshot` を立てると `configHeldBack()` が false になり、次の `saveCfg()` が
この端末の初期値をグループ全体へ配る。**この作りで繰り返し起きてきた事故と同じ道すじ。**

読めない理由は2つに分けて出す。混ぜると、正しい合言葉を持つ人が何度も入れ直す。

- 平文のまま置かれた文書 → 「このグループは古い方式で保存されています…」
- 鍵の不一致 → 「合言葉がちがうため、中身を読めません」

`verifyHousehold()` は `{found:true, config:null, unreadable:true}` を返すことがある。
**呼び出し側2か所（初期設定・設定ページ）で必ず見ること。** 見落とすと
「接続しました ✓」の後に参加できない行き止まりになる（実際にそうなった）。

**旧方式（合言葉をそのまま文書IDにする）は廃止した。** `hashPart`/`legacyHouseIdFor`/
`mayUseLegacy` は削除済み。鍵のない旧文書は読めないので、読み取りだけ残しても
「見つかったのに読めない」になるだけ。旧データは書き出し／読み込みで移す。

**2. 大人向け3ページ化**

`#settings`（保護者用ページ）/ `#tasks`（宿題を決める・新設）/ `#config`（アプリの設定）。
`adultNavHTML()` のインデックスタブで行き来する。子ども画面へのリンクは行末に小さく。

- `TABS` に `'tasks'` を追加。`isAdultTab()` に3つをまとめてある。
  **大人向けページを足すときは `isAdultTab()` にも足すこと**（忘れるとかな変換がかかる）
- 宿題ページは4区分（必ず行う／次に行う／読書／毎日）。必須・任意は独立した欄ではなく
  課題の `group` 値だが、並べかえのまとまり（`taskOrderBucket`）も `group` 単位なので
  画面を分けたほうが実際の動きと合う

**3.「家庭」→「グループ」（294か所）**

合言葉でつながる相手は同居家族とは限らないため。`家庭用版`（＝このリポジトリの
私用配信）だけは別の意味なので残してある。**一括置換するときは保護すること。**

### 実機で出て直した不具合（原因が非自明なものだけ）

| 症状 | 本当の原因 |
|---|---|
| 並べ替えで別の項目が展開 | `detailsKey()` が `<summary>` の文字を鍵にしていた。既定名「おてつだい」が重複し同名行が全部開く。`data-details-key` に課題idを入れて解決 |
| まいにちを 1→0 に戻せない | 「空保存を防ぐ」ガードが打ち消し操作まで遮断。記録がある場合だけ確認して通す |
| 削除しても既定の宿題が復活 | ページ分割で `#cfgShowDaily` が消え `bindConfig` が null 例外 → **以降の全ボタンが未バインド**。保存されず再読込で `freshConfig()`。`on()` ヘルパ経由に変更 |
| QR参加でまいにちが消える／デザインが移らない | 作成直後にQRを読むと「文書はあるが config 未着」の snapshot が1回来る。そこで初期設定を `saveCfg()` して参加端末の初期値がグループになっていた。`applyRemote` の冒頭で `if(!remote.config) return` |
| **親端末のデータが全消し** | 子端末が持ち込んだ `resetAt`（記録をすべて削除の世代番号）。`mergeState` は resetAt が新しい側だけを現世代とし、**古い側を `emptyState()` に置き換える**。別グループへつなぐ時に `resetAt` を手放すようにした |
| 解除しても古い合言葉が残る／おまかせで生成されない | 表示用と入力用が同じ id を共有し、`restoreFormDraft` が古い値を書き戻していた。id を分け、**読み取り専用欄は下書き復元の対象外**に |
| 解除・作り直しをしても元のグループに戻る | ホーム画面版は**起動URLに合言葉が焼きついている**。`K_CODE_CHOSEN`（人が選んだ合言葉）を記録し、URLより優先する |
| 親端末を選んでも子ども画面が開く | 起動画面が `location.hash` だけで決まっていた。iOSが記録するURLに `#` が残るかは制御できない。`routeFromHash()` で `K_ROLE === 'parent'` も見る |
| ベリーのアイコンが出ない | `mask:url("berry.svg")` に版が無く、端末が前のファイルを使い続けていた。**mask の URL にも `?v=` を付けること** |

### `mergeState` について（優先順位を選ばせる必要はない）

実測済み。合流であって勝者総取りではない。

- 進みぐあいは**値ごとに時刻**を持って合流（`mergeProgress`/`pickStamped`）。
  印が無ければ `Math.max` で大きい方
- 記録・本・メッセージは id で合流
- 4通り（どちらが local か × どちらが新しいか）すべてで両側のデータが残ることを確認

**勝者総取りは `resetAt` だけ。** 原則はこう：

> 設定（名前・宿題・デザイン）はグループが勝つ。記録は合流し、どちらも消えない。
> 削除の宣言はグループをまたがない。

「どちらを優先するか」を利用者に選ばせると操作が増え、間違って選べる余地が生まれる。

### デザインの決めごと

- **バーは1色の濃淡で範囲を表す。** うすい緑＝ぜんぶ、こい緑＝かならず やる。
  保護者ページの「つぎに やる」も黄色をやめて濃淡にそろえた。黄色は見出しの色として残す
  （見出しはラベル、バーは進みぐあいで役割がちがう）
- **「全体の進捗」は色ではなく形で分ける。** 子ども画面のバーをそのまま持ちこむ
  2枚がさね（`pstatRow` の第5引数 `inner`）。3本が「全体＝必須＋つぎにやる」と読める
- ネコは帯だけでなく見出しの色みもテーマに寄せた。意味のわけかた（赤／黄／紫／緑）は
  全テーマ共通のまま。全テーマ・全項目で WCAG AA（4.5）以上を実測確認
- テーマごとのしるし：ネコ=paw、おひさま=sun、ソーダ=soda、ベリー=berry（さくらんぼ）、
  ブロック=block。ノートだけ4色の帯のまま（見出しシールに見立てている）

### スピンオフ「かなにする」（`kana.html` + `assets/kana.js`）

声で入れた文を、指定学年までの漢字だけ残してひらがなに直す単体ページ。
**しゅくだいノートとは別の主体**として作ってある。同期も保存も持たない。
`assets/kanji.js` だけを使う（辞書はワーカーの中。入力は端末から出ない）。

- 学年は**小1・小2のみ**。`kanji.js` に配当表がその2学年分しかないため。
  小3〜小6を足すには約740字が要る。**記憶で書くと誤変換が黙って混入する。**
  文科省の学年別漢字配当表を出典として確認してから足すこと
- 辞書なしでも印だけは出る。変換に失敗しても元の文と印を残して手直しできる

### 残っている宿題

1. **IPADIC の配布条件の原文**を上流から取って同梱する。辞書はバイナリで条文が
   入っていない。NOTICE に出自は書いてある。公開後でも対応できる
2. **「きょうはなんの日？」** — 一時保留。経過は `docs/kinenbi-research.md` に保存。
   裏どり済み6件・未確認15件、下書きの誤り率33%（実測）、再開時の注意まで書いてある
3. **3か月無操作で全データ削除** — 実装前に決めること3点：「操作」の定義（開いた時か
   記録した時か）／削除前の予告の有無／端末と Firestore のどちらを基準にするか
4. **記録のまとめ出力機能** — 後日
5. 子ども向けの「おうち」（34行）は残してある。グループ運用に合わせるなら要判断
6. `.task-count` が子ども画面で10pxはみ出す（このセッション以前から。ページ自体は
   横スクロールしないため実害なし）

### 個人情報の扱い

合言葉・家族名・学校名・記録内容はリポジトリにも報告にも書かない。
このセッション中、会話に実際の合言葉が2回混入した（本文とスクリーンショット）。
**依頼者には作り直しを勧めてある。**

## 2026-08-11 Codex追記（保護者ページの省スペース化・進捗判定）

配信予定のキャッシュ版は app / style / tokens / kanji ともに `20260811s`。
回帰テストは99件。保護者ページをローカルで描画し、コンソールエラーなしを確認した。

- 保護者ページの課題名と本の題名は、ともに17pxへそろえた。通知（toast）は16pxへ縮小。
- 「保護者用ページ」と共有状態のバッジを同じ行に置き、タイトル帯の高さを削減した。
  せまい画面では既存どおり共有状態を省略表示する。
- 「夏休みの残り」を「子どもへのメッセージ」より上へ移動。メッセージ本文は17px・
  入力欄は48pxを最小高にして、入力時だけ144pxまで伸びる。
- 画面末尾のクレジットは `© 2026 moyashimisosoup` の後で改行し、中黒を表示しない。
- 子ども画面のペース表示は、**全体進捗と必須進捗がともに夏休み経過を8ポイント以上
  上回る場合だけ**「よゆう」を出す。全体が先行しても必須が夏休み経過を下回る場合は、
  「かならず やる」を先にする案内を出す。各状態は4種類の励まし文から、進捗に応じて
  選ばれる。

## 2026-08-11 Codex追記（完了予測・文字階層・デザイン監査）

配信前のキャッシュ版は app / style / tokens / kanji ともに `20260811t`。回帰テストは
100件。まだコミット・公開していない。

- 全体（必須＋任意、毎日の項目は除外）の、開始日から現在までの平均進捗を使い、
  完了日を予測する。2日以上かつ3%（最低2単位）以上の実績があると日付を出す。
- 実績が少ないときは「計算中」とせず、保護者には「進捗が増えると予測できます」、
  子どもには「すすむと めやすが でるよ」と表示する。
- 子ども画面はペース判定の直下、保護者画面は「夏休みの残り」枠内に、12pxの小さな
  紙ラベルとして配置。吹き出しの尾は、発話者がいない装飾になるため付けていない。
- 保護者ページの「今日の記録」の課題名を17pxにし、下の宿題一覧・本の題名と統一。
- 狭い画面の未共有バッジは「共有なし：設定」に短縮。通常幅の案内文は維持した。
- ペースメッセージは各状態8案、合計32案へ増補。同じ日は安定し、日付と進捗が変わると
  別案が選ばれる。
- 320 / 375 / 414 / 768pxで親子両画面を確認。横スクロールなし。320pxでも予測ラベルは
  1行・約25px高に収まる。`html` と `body` の横方向は `overflow-x:clip` に統一した。

## 2026-08-11 Codex追記（記録単位の表記・保持期限第1段階）

配信前のキャッシュ版は app / style / tokens / kanji ともに `20260811u`。UI回帰テスト
101件、保持期限専用テスト5件が通過。保持期限のFunctionとルールは**まだ未デプロイ**。

- 本の記録は丸数字をやめ、一覧・記録シート・ログを `1冊` / `何冊目の本？` /
  `1冊目` に統一。単位が「枚」の課題も算用数字と `何枚目までやった？` を使う。
  それ以外の `numbered` 課題は従来どおり丸数字。
- 6回以上の毎日の項目は、具体的な回数入力を促す。入力欄は1〜5のボタンと同じ
  1グリッド分・高さ76px・数字30px。回以外の単位も案内文を自動調整する。
- 記録シートは開いた直後にも `applyReadingDisplay($('#sheetWrap'))` を通す。子どもの
  漢字設定が低学年なら `冊` / `枚` / `以上` も既存辞書でかな表示になる。
- 保護者メッセージの差出人欄と本文欄は外寸48px・17pxに統一。textarea は
  `box-sizing:border-box` が必要（無いとpadding込みで67pxになった）。
- 子どもの完了予測は紙ラベルをやめ、`このペースなら8月10日におわりそう。` の通常文。
  保護者用の小ラベルは変更していない。

保持期限第1段階のソースは `functions/`、`firebase.json`、`firestore.rules` にある。
config/stateの暗号文がサーバーで受理された変更だけを活動とし、サーバーイベント時刻から
`lastActivityAt` / `expiresAt` / `retentionVersion` / `retentionEligibleAt` を記録する。
devices更新・snapshot・Function自身の書き戻しは活動に数えない。削除・墓標・警告は未実装。

**デプロイ順を守ること。** Blazeプランと権限を確認し、まず
`firebase deploy --only firestore:rules`、反映確認後に
`firebase deploy --only functions:recordHouseholdActivity`。無指定の `firebase deploy` は使わない。
詳細は `functions/README.md`。npm auditのmoderate 7件は現行SDKの推移依存 `uuid` 由来で、
現時点では非破壊の自動修正なし。

## 2026-08-11 Codex追記（手動の共有データ削除）

配信前のキャッシュ版は app / style / tokens / kanji / sync ともに `20260811v`。UI回帰
102件、保持期限・管理ツールの専用テスト7件が通過。

- `household_tombstones/<共有IDのSHA-256>` を削除処理中の印にする。クライアントは接続・
  参加確認・常時監視でこの印を先に見る。見つけた端末は古い保留送信を捨て、端末内の
  旧データと合言葉を消して、`この共有データは削除処理中のため、もう使えません。`
  `新しい合言葉で始めてください。` と初期登録画面で案内する。
- `firestore.rules` の `notRetired()` は墓標がある共有IDの create / update を拒否する。
  これがないと、古いオフライン端末が本体を復活させる。**管理ツールを使う前に、必ず
  Firestore Consoleへ今回の rules を反映すること。** ルール未反映でも新アプリは通常同期を
  続けるが、手動削除の復活防止はまだ効かない。
- 管理者PCだけで使う簡易UIは `tools/retention-admin.js`。ブラウザ権限や秘密鍵を公開サイトへ
  置かず、`127.0.0.1` で開く。共有IDを貼って削除候補を確認し、`削除 N 件` の入力と確認後に
  「墓標作成＋本体削除」を同じFirestoreバッチで行う。手順・鍵の扱いは
  `tools/RETENTION_ADMIN.md` を参照。Cloud Functions / Schedulerは使わない。

## 2026-08-11 Codex追記（冊目表記・任意の名前・90日注記・起動表示）

公開コードコミットは `170fb73`。キャッシュ版は app / style / tokens / kanji / sync ともに
`20260811w`。全テスト107件、320px / 375pxの実画面確認、横スクロールなし、コンソールの
警告・エラーなし。

- 読書の序数は `bookCountUnit()` / `bookOrdinal()` に一本化。子ども画面は「漢字のまま」
  だけ `1冊目`、ひらがな対応モードは `1さつめ`。保護者ページと進捗サマリーは常に
  `1冊目`。読書シートの重複した質問「何冊目の本？」はやめ、`1冊目の本` / `1さつめのほん`
  の1表示にした。子どもの本一覧も `1冊` ではなく序数で表示する。
- 子どもの名前は新規作成・既存共有への参加・端末だけで使う場合・設定ページのすべてで
  任意と表示。空欄のまま開始でき、空欄へ戻した場合は端末に古い名前を残さない。
- 登録時の共有注意事項に、共有データが「どの端末からも更新されない期間」を基準に
  90日後の管理者確認対象となることを12pxで表示。閲覧だけでは延長せず、共有しない
  端末内データは対象外と明記した。現状は手動運用なので「自動削除」とは書かない。
- 共有中の子ども端末だけ旧データを自動保持する仕様は採用しない。役割が端末の自己申告で、
  オフライン世代差や新しい共有への旧データ混入を起こしうるため。共有なしの端末内利用は
  そのまま残るが、共有中の世帯は墓標後に全役割で同期停止・旧合言葉解除を維持する。
- 起動時の白画面を、静的HTMLの「読み込み中…10%」とCSSだけのバーへ置換。各同期scriptの
  読込完了段階で25 / 40 / 55 / 75 / 88 / 100%へ進むため、架空の時間カウントではない。
  待ち時間を足す処理はなく、外部Webフォントも非同期化して初期描画をブロックしない。

## 2026-08-11 Codex追記（数字と単位の揃え・完了予測の明示化）

公開コードコミットは `45432da`。キャッシュ版は app / style / tokens / kanji / sync ともに
`20260811x`。全テスト107件、320px実画面で横スクロール・コンソールエラーなし。

- 子どもカードの「つぎは 10 さつめ」「つぎは 2 まいめ」などを、案内・大きい数字・
  単位の3要素に統一。中央揃えではなく文字ベースライン揃えを採用し、数字と単位の下端が
  自然にそろうようにした。丸数字・段階名を含む同じ表示経路へ一括適用される。
- 子どもの日付あり予測を `かんりょうよそく：いまのペースだと8月10日` に変更。
  夏休み終了日で予測日を丸めたり隠したりせず、9月以降もそのまま表示するテストを追加した。
  320pxではコロン後を折り返し位置にし、日付の途中は折らない。通常幅では1行になる。
- 全ひらがな設定で数値日付の「月」が辞書により「つき」となっていたため、`8がつ11にち`
  となる文脈補正を追加。小学1年生以上は既習漢字として `8月11日` のまま残す。

## 2026-08-11 Codex追記（毎日の項目の説明整理）

公開コードコミットは `6e5c1ac`。キャッシュ版は app / style / tokens / kanji / sync ともに
`20260811y`。全テスト107件が通過。

- 「毎日の項目」は表示切替の直下の説明だけを残し、一覧の前にあった重複説明を削除。
  文言は `学習アプリ・音読・おてつだい・日記やメモなどに使えます。`。ほかの3欄は
  既存どおり、用途と並び替え方法の案内を表示する。

## 2026-08-11 Codex追記（保護者ページ上部の整列）

公開コードコミットは `bc290a4`。キャッシュ版は app / style / tokens / kanji / sync ともに
`20260811z`。全テスト107件、320 / 375 / 414 / 768 / 1170pxの実画面で横スクロールなし、
コンソールの警告・エラーなし。

- `子ども画面へ`を保護者向け3タブと同じ段へ移し、3タブは等幅、子ども画面への導線は
  内容幅の副ボタンにした。320pxでも4つとも44pxのタップ高と1行表記を保つ。
- 共有状態ボタンはタイトル直後ではなく、`保護者用ページ`見出し行の右端へ寄せた。
  共有なし・共有中・接続待ちの各文言や動作は変更していない。
